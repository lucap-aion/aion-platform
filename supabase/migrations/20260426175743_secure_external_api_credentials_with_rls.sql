-- Placeholder: this migration was applied directly to PROD before the local
-- repo started tracking migrations, so there is no DDL here. The stub exists
-- only so 'supabase db push' can reconcile the tracker against local files.
-- Idempotent no-op; do not delete.
select 1 where false;
